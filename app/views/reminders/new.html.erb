<h1>Send Notification</h1>

<%= form_tag reminder_path, :method => :post do %>
    <div class="field">
      <%= label_tag :name %>
      <%= text_field_tag :name, params[:name] %>
    </div>
    <div class="actions"><%= submit_tag "Send Notification" %></div>
<% end %>